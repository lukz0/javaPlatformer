public class EliteAI extends GoonAI{
    /*private Mario target;

    EliteAI(Mario target, Enemy parent) {
        this.target = target;
        this.parent = parent;
    }

    void attack() {
        if (target.posY < parent.posY) {
            if (target.posX - parent.posX > 0) {
                parent.vX = -parent.spd;
            }
            else {
                parent.vX = parent.spd;
            }
        }
        else {
            if (target.posX - parent.posX > 0) {
                parent.vX = parent.spd;
            }
            else {
                parent.vX = parent.spd;
            }
        }
    }*/
}
